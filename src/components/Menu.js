import React, { Component } from 'react';
import DataMenu from './DataMenu';

class Menu extends Component {
    render() { 
        return (
            <DataMenu />
        );
    }
}
 
export default Menu;